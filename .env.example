# Dev: backend runs on a separate port → point at it explicitly.
# Prod: leave both empty so the browser uses same-origin URLs (Caddy proxies /api/*).
NEXT_PUBLIC_API_BASE=http://localhost:8000
NEXT_PUBLIC_SSE_BASE=http://localhost:8000
